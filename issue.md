# debata 課題・改善タスク一覧 (Issue)

最終更新日: 2026年8月7日

---

## 🐛 1. 不具合修正・バグ対応 (Bug Fixes)
- [x] **対戦チャットのメッセージ表示崩れ・非表示バグの修正**
  - **症状**: 対戦画面（`GamePage`等）において、メッセージの一部または全文章が途切れたり画面外にはみ出して表示されない。
  - **対応方針**: メッセージバブルの `Text` ウィジェットのレスポンシブ対応、`Expanded` / `Flexible` の配置、スクロール挙動およびオーバーフロー処理の改修。

---

## ⏱️ 2. 仕様変更・基本機能改修 (Core Specs & Rules)
- [x] **対戦（試合）制限時間の10分化**
  - **内容**: 試合時間を **10分間（600秒）** に変更する。
  - **対応方針**: タイマー制御ロジック、画面上部のカウントダウン表示、タイムアップ判定の調整。

---

## 🤖 3. UX向上・マッチング離脱防止 (Matchmaking & Solo AI)
- [ ] **5秒タイムアウトでの「AIソロ対戦モード」自動/ダイアログ移行**
  - **内容**: オンラインマッチング検索を開始後、**5秒間** 相手が見つからなかった場合、スムーズに「AIと対戦する」選択ダイアログを表示（または自動切り替え）して即座に対戦を開始できる仕組み。
  - **効果**: マッチング待ちによるユーザーの離脱をゼロにする。

---

## 📊 4. UI/UX・対戦結果のビジュアル化 (Result UI & Analytics)
- [ ] **試合結果への「論理能力 %表示」＆「レーダーチャート評価」導入**
  - **内容**:
    - **レーダーチャート表示**: 長文の勝敗判定理由だけでなく、「論理性」「説得力」「反論の鋭さ」「構成力」「マナー」等の数値を蜘蛛の巣状のグラフ（レーダーチャート）で一目で視覚化する。
    - **論理能力 %表示**: 「あなたの今回の論理的思考力: 85%」「説得度: 92%」といったパーセンテージ表示を結果画面に追加し、ユーザーの達成感・成長実感を高める。

---

## 📢 5. ユーザー獲得・コミュニティ機能 (User Growth & Board)
- [ ] **対戦申し込み掲示板（コミュニティボード）機能**
  - **内容**: アプリ内にユーザー同士が自由にお題や対戦募集メッセージを投稿できる「掲示板」を作成。
  - **機能**: 掲示板の投稿から直接「この人と対戦する」「挑戦状を送る」ボタンを押して対戦を申し込める導線を作り、ユーザー間の交流と対戦頻度を増やす。

---

## 🚀 6. 本番環境（サーバー）への切り替え・復帰手順 (Production Rollback Checklist)
ローカル開発・検証完了後、アプリおよびSupabaseを本番環境に戻す際の手順一覧。

> [!CAUTION]
> **【超重要】ローカル検証用 ngrok の URL を本番環境（サーバー）に絶対に入れないこと！**
> - 現在、ローカルDBの `handle_room_updates_v2` / `v3` 内の `target_url` には、開発用PCへ転送するための **ngrok のURL (`https://undebilitative-...ngrok-free.dev`)** が設定されています。
> - **万が一、この ngrok URL を本番Supabaseの関数に適用してしまうと、本番の全ユーザーのAI審査がローカルPC宛てに送られてしまい、本番でAI審査が動かなくなる致命的な障害になります。**
> - 本番DBへのマイグレーション反映時や本番運用時は、`target_url` が必ず **本番URL (`https://ljgvqdcailabzuutaeha.supabase.co/functions/v1/gemini_v2`)** になっていることを二重チェックしてください。

### 切り替えチェックリスト
- [ ] **1. `.env` の接続先を本番環境に戻す**
  ```env
  # --- 本番環境設定 ---
  P_VAR_URL='https://ljgvqdcailabzuutaeha.supabase.co'
  P_VAR_ANONKEY='sb_publishable__qaZNRqKG4BwO0V3FSsIXw_azYEwtAN'
  ```
- [ ] **2. データベースファンクションの `target_url` の確認（本番DB）**
  - 本番Supabaseの `handle_room_updates_v2` / `v3` 内の `target_url` が **本番URL（`https://ljgvqdcailabzuutaeha.supabase.co/functions/v1/gemini_v2`）** になっていることを確認（※ngrokのURLが入っていないこと！）。
- [ ] **3. Edge Functions のデプロイ（ローカルで関数修正した場合）**
  ```bash
  npx supabase functions deploy gemini_v2
  npx supabase functions deploy push_notification_v2
  ```
- [ ] **4. ローカル開発環境の停止**
  - ngrok プロセスの停止（ターミナルを終了またはタスク終了）
  - ローカルSupabaseの停止: `npx supabase stop`
- [ ] **5. アプリの接続テスト**
  - アプリを再起動し、本番Supabaseへのログイン・マッチング・データ同期が正常に動作することを確認。


