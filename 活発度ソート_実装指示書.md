# 掲示板「活発度」ソート実装指示書

この指示書に従って、掲示板（BBS）一覧に「新着」だけでなく **「活発度」順のソート** を実装してください。対象は Flutter + Supabase (PostgreSQL) のプロジェクト（`C:\Users\kazuk\program\AppList\debata`）です。

---

## 1. 目的

現在、掲示板の投稿一覧は `get_bbs_posts_with_status` で **新着順（created_at DESC）固定** になっている。

これを以下の4つの並び替えに対応させる：

| ソート | 内容 |
|---|---|
| `new`（デフォルト） | 新着順（現在の挙動のまま） |
| `likes` | イイネ数が多い順 |
| `replies` | 返信数（累計）が多い順 |
| `activity` | **活発度スコア順（今回の新機能）** |

**「活発度」の狙い**：
- 累計の返信数が多くても「昔の盛り上がり」は上位にしない
- **直近に返信が付いている投稿**（例：最後の返信が1時間前）を上に上げる
- 具体的には「**5日前に100返信付いた投稿**」より「**今まさに10返信付いている投稿**」を上に表示する

---

## 2. 前提（現状のデータ構造）

### 2-1. 関連テーブル

```sql
-- 掲示板の投稿
CREATE TABLE public.bbs_posts (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  content text NOT NULL,
  created_at timestamptz DEFAULT now(),
  likes_count integer DEFAULT 0,      -- トリガー/RPCで自動更新済み
  replies_count integer DEFAULT 0,    -- 同上
  image_urls text[],
  is_deleted boolean DEFAULT false
);

-- 掲示板の返信（コメント）。返信の返信（ネスト）もこのテーブル
CREATE TABLE public.bbs_comments (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  post_id uuid NOT NULL,            -- 親投稿
  user_id uuid NOT NULL,
  parent_comment_id uuid,           -- NULL=トップレベル返信, 非NULL=リプライのリプライ
  content text NOT NULL,
  created_at timestamptz DEFAULT now(),
  likes_count integer DEFAULT 0,
  image_url text,
  is_deleted boolean DEFAULT false
);
```

**「返信」の数え方の定義**：既存の `replies_count` はコメント作成のたびに `increment_replies_count` RPC で +1 される（ネスト返信も含む）。**活発度計算でも同じく、`bbs_comments` の行を全部数えること**（`parent_comment_id` で絞らない）。

### 2-2. 既存の取得RPC（現在の形・変更対象）

`supabase/migrations/20260902000000_bbs_soft_delete.sql` にある関数。**この関数を `p_sort` 引数付きに書き換える**：

```sql
CREATE OR REPLACE FUNCTION public.get_bbs_posts_with_status (
  p_user_id uuid,
  p_limit   integer DEFAULT 50
)
RETURNS TABLE (
  id             uuid,
  user_id        uuid,
  content        text,
  created_at     timestamp with time zone,
  likes_count    integer,
  replies_count  integer,
  image_urls     text[],
  users          json,
  is_liked_by_me boolean,
  has_resba      boolean,
  is_deleted     boolean
)
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path TO 'public'
AS $$
BEGIN
  RETURN QUERY
  SELECT
    p.id, p.user_id, p.content, p.created_at, p.likes_count, p.replies_count,
    p.image_urls,
    row_to_json(u) AS users,
    EXISTS(SELECT 1 FROM bbs_likes l WHERE l.post_id = p.id AND l.user_id = p_user_id) AS is_liked_by_me,
    EXISTS(SELECT 1 FROM battle_invites b
            WHERE b.attach_type = 'post' AND b.attach_id = p.id
              AND b.status IN ('pending', 'accepted')) AS has_resba,
    p.is_deleted
  FROM bbs_posts p
  LEFT JOIN users u ON u.id = p.user_id
  ORDER BY p.created_at DESC
  LIMIT p_limit;
END;
$$;

GRANT ALL ON FUNCTION public.get_bbs_posts_with_status(uuid, integer) TO anon;
GRANT ALL ON FUNCTION public.get_bbs_posts_with_status(uuid, integer) TO authenticated;
GRANT ALL ON FUNCTION public.get_bbs_posts_with_status(uuid, integer) TO service_role;
```

### 2-3. 既存インデックス（そのまま使える）

```sql
-- bbs_comments: 投稿ID + 作成日時。LATERAL集計に有効
CREATE INDEX idx_bbs_comments_post_created ON public.bbs_comments (post_id, created_at ASC);
```

---

## 3. 活発度スコアの計算式（コア仕様）

### 3-1. 定義

各投稿について、以下の3つを求める：

| 値 | 定義 |
|---|---|
| `recent_cnt` | その投稿の返信のうち、**直近24時間以内**（`created_at > now() - interval '24 hours'`）に付いた件数 |
| `last_at` | その投稿の返信の最新 `created_at`（**全期間**で見て最後の返信時刻） |
| `hours_since_last` | `(now() - last_at)` を**時間単位**にした値 |

**活発度スコア**：

```
activity_score = recent_cnt ÷ (hours_since_last + 2)
```

ルール：
- `recent_cnt = 0`（直近24時間に返信がない）→ **スコア0**
- 返信が1件もない投稿 → last_at の代わりに `p.created_at` を使う（COALESCE）→ スコア0
- スコア0の投稿は「圏外」扱い。リストの最下部に回す

### 3-2. 計算例（動作確認用の期待値）

| ケース | recent_cnt | hours_since_last | スコア |
|---|---|---|---|
| 直近24hに10返信・最後の返信が30分前 | 10 | 0.5 | 10 ÷ 2.5 = **4.00** |
| 5日前に100返信＋今10返信・最後1時間前 | 10 | 1.0 | 10 ÷ 3 = **3.33** |
| 3返信・最後2分前 | 3 | 0.033 | 3 ÷ 2.033 = **1.48** |
| 新着2返信・最後5分前 | 2 | 0.083 | 2 ÷ 2.083 = **0.96** |
| 10返信・最後20時間前 | 10 | 20 | 10 ÷ 22 = **0.45** |
| 5日前に100返信・直近0件 | 0 | — | **0（圏外）** |

**重要な性質**：
1. 時間が経つと（何も起きなくても）スコアは下がる → 古い盛り上がりは自然に沈む
2. 新しい返信が来れば再浮上する（活発な投稿だけが生き続ける）
3. 分母の `+2` は「返信直後のスコア爆発」を抑える緩衝。`^1.8` など累乗にすると新しさの効きが急峻になる（調整用つまみ）

### 3-3. 調整用パラメータ（すべて定数、実装時はこの値で）

| パラメータ | 値 | 意味 |
|---|---|---|
| 窓（ウィンドウ） | `24 hours` | 「直近」の範囲。大きくすると日単位、小さくすると「今まさに」 |
| 分母の `+2` | `2` | 返信直後のスコア上限を抑える緩衝定数 |
| 指数 | `1`（=なし） | 新しさの効きの強さ。必要なら `(hours+2)^1.8` に変更可能 |

**設計上の注意：このスコアは「リクエスト時に毎回計算」するのが正解**（Hacker News / Reddit と同じ方式）。
理由：時間減衰スコアは「何も起きなくても時間経過で変わる」ため、トリガーだけでは維持できない。専用テーブルやバッチ（5分ごとの再計算）は**今の規模では不要**。候補は `LIMIT 50` 件程度なので、1画面取得あたりの計算コストは無視できる。

---

## 4. 実装手順

### Step 1: SQL（Supabaseマイグレーション）

`supabase/migrations/` に新規マイグレーションファイル（例：`20261101000000_bbs_activity_sort.sql`）を作成する。

#### 4-1. `get_bbs_posts_with_status` を `p_sort` 対応に書き換え

```sql
DROP FUNCTION IF EXISTS public.get_bbs_posts_with_status(uuid, integer);

CREATE OR REPLACE FUNCTION public.get_bbs_posts_with_status (
  p_user_id uuid,
  p_limit   integer DEFAULT 50,
  p_sort    text DEFAULT 'new'   -- 'new' | 'likes' | 'replies' | 'activity'
)
RETURNS TABLE (
  id             uuid,
  user_id        uuid,
  content        text,
  created_at     timestamp with time zone,
  likes_count    integer,
  replies_count  integer,
  image_urls     text[],
  users          json,
  is_liked_by_me boolean,
  has_resba      boolean,
  is_deleted     boolean
)
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path TO 'public'
AS $$
BEGIN
  RETURN QUERY
  SELECT
    p.id,
    p.user_id,
    p.content,
    p.created_at,
    p.likes_count,
    p.replies_count,
    p.image_urls,
    row_to_json(u) AS users,
    EXISTS(SELECT 1 FROM bbs_likes l WHERE l.post_id = p.id AND l.user_id = p_user_id) AS is_liked_by_me,
    EXISTS(SELECT 1 FROM battle_invites b
            WHERE b.attach_type = 'post' AND b.attach_id = p.id
              AND b.status IN ('pending', 'accepted')) AS has_resba,
    p.is_deleted
  FROM bbs_posts p
  LEFT JOIN users u ON u.id = p.user_id
  -- 活発度用: 投稿ごとに「直近24hの返信数」と「最後の返信時刻」を1パスで集計
  LEFT JOIN LATERAL (
    SELECT
      COUNT(*) FILTER (WHERE c.created_at > now() - interval '24 hours') AS recent_cnt,
      MAX(c.created_at) AS last_at
    FROM bbs_comments c
    WHERE c.post_id = p.id
  ) a ON true
  ORDER BY
    CASE p_sort
      WHEN 'new'      THEN EXTRACT(EPOCH FROM p.created_at)::double precision
      WHEN 'likes'    THEN p.likes_count::double precision
      WHEN 'replies'  THEN p.replies_count::double precision
      WHEN 'activity' THEN (a.recent_cnt::double precision / (
                            EXTRACT(EPOCH FROM (now() - COALESCE(a.last_at, p.created_at)))::double precision / 3600.0 + 2.0))
      ELSE EXTRACT(EPOCH FROM p.created_at)::double precision
    END DESC NULLS LAST
  LIMIT p_limit;
END;
$$;

GRANT ALL ON FUNCTION public.get_bbs_posts_with_status(uuid, integer, text) TO anon;
GRANT ALL ON FUNCTION public.get_bbs_posts_with_status(uuid, integer, text) TO authenticated;
GRANT ALL ON FUNCTION public.get_bbs_posts_with_status(uuid, integer, text) TO service_role;
```

補足：
- 引数が3つに増えるため、**GRANT も3引数版で付け直す**こと（`DROP FUNCTION` で2引数版は消える）
- `ORDER BY` は CASE で全ソートを1つの数値式（`double precision`）に統一。NULLは `NULLS LAST` で最下部
- `p_sort = 'activity'` のとき、スコア0（直近返信なし）の投稿は「最下部に来る」形で表示される（＝リストから消さず下に沈める）。**完全に非表示にしたい場合**は `WHERE (p_sort <> 'activity' OR a.recent_cnt > 0)` を追加する（デフォルトは最下部回し）

#### 4-2. `last_reply_at` 列の追加（推奨・任意）

表示バッジ「最後の返信: ◯時間前」や、将来の最適化のために `bbs_posts.last_reply_at` を持っておくと便利。既存の `member_count`・`likes_count` と同じ「トリガーで事前更新」パターン：

```sql
ALTER TABLE public.bbs_posts ADD COLUMN IF NOT EXISTS last_reply_at timestamptz;

-- バックフィル（既存データ）
UPDATE public.bbs_posts p
SET last_reply_at = (SELECT MAX(c.created_at) FROM bbs_comments c WHERE c.post_id = p.id);

CREATE OR REPLACE FUNCTION public.update_bbs_last_reply_at()
RETURNS TRIGGER
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path TO 'public'
AS $$
BEGIN
  UPDATE public.bbs_posts SET last_reply_at = NEW.created_at WHERE id = NEW.post_id;
  RETURN NEW;
END;
$$;

DROP TRIGGER IF EXISTS trg_bbs_last_reply_at ON public.bbs_comments;
CREATE TRIGGER trg_bbs_last_reply_at
AFTER INSERT ON public.bbs_comments
FOR EACH ROW
EXECUTE FUNCTION public.update_bbs_last_reply_at();
```

（この列は**今回のソート実装には必須ではない**。ソートは上記の LATERAL 集計だけで動く。列は表示用・将来用として追加する）

### Step 2: Flutter 側の変更

#### 2-1. ソート状態のProvider追加 — `lib/provider/bbs_timeline_provider.dart`

```dart
// 上部に追加: ソート選択状態 ('new' | 'likes' | 'replies' | 'activity')
final bbsSortProvider = StateProvider<String>((ref) => 'new');
```

`fetchPosts()` 内の RPC 呼び出しに `p_sort` を渡す：

```dart
final response = await supabase.rpc('get_bbs_posts_with_status', params: {
  'p_user_id': currentUserId,
  'p_sort': _ref.read(bbsSortProvider),
});
```

#### 2-2. 一覧画面にソート切り替えUI — `lib/views/bbs/BbsTimelineView.dart`

`ListView` の上に、以下のようなトグル（チップ/セグメント）を1行追加する：

```dart
// 例: ソート切り替え
Row(
  children: [
    _SortChip(label: '新着', value: 'new'),
    _SortChip(label: 'イイネ', value: 'likes'),
    _SortChip(label: '返信', value: 'replies'),
    _SortChip(label: '活発', value: 'activity'),
  ],
)
```

チップ選択時の挙動：
1. `ref.read(bbsSortProvider.notifier).state = 選択値`
2. `ref.read(bbsTimelineProvider.notifier).fetchPosts()` を呼んで再取得

（デザインは既存のタブ切り替え（`Tab(text: '新着')` 等）やアプリの既存スタイル `AppTextStyles` に合わせること）

#### 2-3. （任意）表示バッジ

「活発」タブで表示する場合、各投稿に「直近24hの返信数」や「最後の返信から◯時間」を出すと分かりやすい。`lib/modes/bbs_post.dart` にフィールドを追加するか、`BbsPostWidget` 内で時刻差を計算して表示する。

---

## 5. 検証方法（完了条件）

### 5-1. SQL レベルの検証

Supabase ローカル（`supabase db query` または psql）で、以下の**期待挙動**を確認する：

1. `p_sort = 'new'` → 従来と同じ新着順（**回帰なし**）
2. `p_sort = 'likes'` → `likes_count` 降順
3. `p_sort = 'replies'` → `replies_count` 降順
4. `p_sort = 'activity'` → 以下を確認：
   - 「直近24hに10返信・最後の返信1時間前」の投稿が、「5日前に100返信・直近0件」の投稿より**必ず上位**
   - 最後の返信が新しい投稿ほど上（最終返信30分前 > 1時間前 > 20時間前）
   - 直近24hに返信がない投稿（累計が多くても）は最下部に沈む
   - `LIMIT 50` が効いている

テストデータ例（期待値）：
- 投稿A: 返信100件（5日前）＋返信10件（直近24h・最後は1時間前）→ スコア3.33
- 投稿B: 返信10件（直近24h・最後は30分前）→ スコア4.00
- 投稿C: 返信100件（5日前のみ・直近0件）→ スコア0
- 並び: **B > A > C**

### 5-2. アプリレベルの検証

1. 掲示板タブで「活発」を選択 → 直近に返信が付いた投稿が上位に並ぶ
2. 他のタブ（新着/イイネ/返信）との切り替えで正しく再取得される
3. 既存の「新着」（デフォルト）表示は今までと変わらない

### 5-3. シミュレーション参考値（この式で期待される時系列挙動）

返信が来ない投稿は**24時間以内にスコア0（圏外）**になる。新しい返信が来れば復活する。
例（P1 = 5日前に100返信＋直近10返信の投稿）：

```
時間経過:   0h   6h    12h   24h   36h  48h   72h
スコア:   3.33  0.89  0.40  0.00  0.00  -     -
48h後に新返信10件 → 4.00 に復活
```

---

## 6. 注意事項・落とし穴

1. **RPCのシグネチャ変更**：`get_bbs_posts_with_status` が2引数→3引数になる。Flutter側の呼び出し（`bbs_timeline_provider.dart`）と、GRANT 文を必ず更新する。`DROP FUNCTION` と再作成の順序に注意（`DROP FUNCTION IF EXISTS ... (uuid, integer)` は既存シグネチャで落とす）
2. **SECURITY DEFINER 関数なので**：`SET search_path TO 'public'` を付ける（既存関数の作法に倣う）。RLSを迂回するため、認可ロジックを足さないこと（既存と同じ「誰でも閲覧可」のままでよい）
3. **ネスト返信の扱い**：`bbs_comments` はリプライのリプライも含む。既存 `replies_count` と定義を揃えるため**全件を数える**。`parent_comment_id` で絞らない
4. **スコアはリクエスト時計算でOK**：専用テーブルやバッチ再計算は作らない（データが数千件を超えて遅くなってから検討）
5. **インデックス**：既存の `idx_bbs_comments_post_created (post_id, created_at)` で動く。もし遅くなったら `CREATE INDEX idx_bbs_comments_created_post ON bbs_comments (created_at, post_id)` を追加（参考情報。今は不要）
6. **「圏外」の扱い**：デフォルトは「スコア0は最下部に回す（非表示にしない）」。完全非表示にしたい場合は 4-1 の WHERE オプションを使う（プロダクト判断で選択）
7. **論理削除との整合**：`is_deleted = true` の投稿は既存処理でプレースホルダー表示される。ソート計算は `bbs_comments` がベースなので影響なし。ただし削除済み返信（`c.is_deleted = true`）を数えるかどうかは、既存の `replies_count` の挙動（削除時も減らない）に合わせて**数えたまま**でよい

---

## 7. 成果物リスト

- [ ] 新規マイグレーション SQL（`get_bbs_posts_with_status` の3引数化＋sort対応）
- [ ] （任意）`last_reply_at` 列＋トリガー＋バックフィル
- [ ] `lib/provider/bbs_timeline_provider.dart` の変更（`bbsSortProvider` 追加・`p_sort` 渡し）
- [ ] `lib/views/bbs/BbsTimelineView.dart` の変更（ソート切り替えUI）
- [ ] （任意）`lib/modes/bbs_post.dart` の表示バッジ対応
- [ ] SQL・アプリ両方の動作検証（5章の完了条件をすべて満たす）